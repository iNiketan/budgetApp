.expo/
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.DS_Store

# Environment variables — .env holds the sheet password and the app PIN.
# Never commit it. .env.example is the tracked template.
.env
.env.*
!.env.example

# IDE / editor
.claude/

# Build artifacts
*.apk
*.aab
build-*
dist/
coverage/

# Native projects — generated by `expo prebuild` (Continuous Native Generation).
# EAS Build regenerates these, so they are not source.
/android
/ios

# Jest
.jest-cache/
